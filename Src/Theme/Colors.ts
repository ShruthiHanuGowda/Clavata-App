export type Colors = {
  white: string;
  error: string;
  success: string;
  purple: string;
  black: string;
};

const colors: Colors = {
  white: '#fff',
  error: '#FF9494',
  success: '#009D94',
  purple: '#1E295A',
  black: '#000000',
};

export default colors;
